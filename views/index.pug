extends layout

block content
  h1= title
  p Welcome to #[em Siblance Music Store], a very basic Express website developed as practive for MERN stack.

  h1 Dynamic content

  if error
    p Error getting dynamic content.
  else
    p Store has the following items:

    ul
      li #[strong Items:] !{data.item_count}
      li #[strong Categories:] !{data.category_count}
